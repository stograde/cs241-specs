# F21
---
assignment: lab6

compilers: 
  - &c 'gcc $@ -o $@.exec'
  - &c-math 'gcc $@ -o $@.exec -lm'

files:
  - file: trybin.c
    commands: *c
    tests: $@.exec
  - file: getbit.c
    commands: *c
    tests: $@.exec
  - file: strncopyp.c
    commands: *c
    tests: $@.exec
  - file: strindexp.c
    commands: *c
    tests: $@.exec
  - file: fact3.c
    commands: *c-math
    tests: echo 5 | $@.exec
