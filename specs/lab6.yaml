---
assignment: lab6

compilers: 
  - &c 'gcc $@ -o $@.exec'
  - &c-pthread 'gcc $@ -lm -lpthread -o $@.exec'

files:
  - [ hello.c, *c ]
  - [ hello2.c, *c ]
  - [ pthreads1.c, *c-pthread ]

tests:
  - [ hello.c, $@.exec ]
  - [ hello2.c, $@.exec ]
  - [ pthreads1.c, $@.exec ]
