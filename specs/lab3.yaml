# F21
---
assignment: lab3

compilers:
  - &c 'gcc $@ -o $@.exec'
  - &c-math 'gcc $@ -lm -o $@.exec'
  - &c-thread-math 'gcc $@ -lpthread -lm -o $@.exec'

files:
  - file: pthreads.c
    commands: *c-thread-math
    tests: $@.exec
    options:
      optional_compile: true
  - file: example2.c
    commands: *c-math
    tests: $@.exec
  - file: sqrts.c
    commands: *c-math
    tests: $@.exec
  - file: fact.c
    commands: *c-math
    tests: $@.exec
  - file: sqrtsF.c
    commands: *c-math
    tests: $@.exec
  - file: factF.c
    commands: *c-math
    tests: $@.exec
  - file: fact2.c
    commands: *c-math
    tests: echo 5 | $@.exec
  - file: sqrtsF.c
    commands: *c-math
    tests: $@.exec
  - file: factF.c
    commands: *c-math
    tests: $@.exec
  - file: prod_array.c
    commands: *c
    tests: $@.exec
  - file: prod_array2.c
    commands: *c
    tests: $@.exec
  - file: sum_array.c
    commands: *c
    tests: $@.exec

