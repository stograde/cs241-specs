---
assignment: lab2

compilers:
  - &c-math 'gcc $@ -o $@.exec -lm'

files:
  - file: example.c
    commands: *c-math
    tests: $@.exec
  - file: example1.c
    commands: *c-math
    tests: $@.exec
  - file: sqrt0.c
    commands: *c-math
    tests: $@.exec
  - file: tests.c
    commands: *c-math
    tests: $@.exec
  - file: tests2.c
    commands: *c-math
    tests: $@.exec
  - file: id_rsa.pub
  