# F22
---
assignment: lab2

compilers:
  - &c-math 'gcc $@ -o $@.exec -lm'

files:
  - file: example2.c
    commands: *c-math
    tests: $@.exec
  - file: sqrts.c
    commands: *c-math
    tests: $@.exec
  - file: fact.c
    commands: *c-math
    tests: $@.exec
  - file: fact2.c
    commands: *c-math
    tests: echo 5 | $@.exec
