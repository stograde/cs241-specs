---
assignment: lab4

compilers:
  - &c-math 'gcc $@ -lm -o $@.exec'

files:
  - [ sqrts.c, *c-math ]
  - [ fact.c, *c-math ]

tests:
  - [ hello.c, $@.exec ]
  - [ hello2.c, $@.exec ]
  