---
assignment: hw1

compilers:
  - &c 'gcc $@ -o $@.exec'
  - &c-math 'gcc $@ -o $@.exec -lm'

files:
  - file: id_rsa.pub
  - file: copy_array.c
    commands: *c
    tests: $@.exec
  - file: strncopy2.c
    commands: *c
    tests: $@.exec
  - file:  testinterest.c
    commands:  *c-math
    tests: $@.exec
