---
assignment: lab10

compilers: 
  - &c-s 'gcc -c -S $@ -lpthread -lm'
  - &c-thread-math 'gcc $@ -o $@.exec -lpthread -lm'
  - &c-math 'gcc $@ -o $@.exec -lm'

files:
  - [ sqrts2.c, *c-s ]
  - [ sqrts2.s ]
  - [ prod_array.c, *c-math ]
  - [ mean_array.c, *c-math ]

tests:
  - [ prod_array.c, $@.exec ]
  - [ mean_array.c, $@.exec ]