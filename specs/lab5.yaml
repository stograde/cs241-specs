---
assignment: lab5

compilers:
  - &c 'gcc $@ -o $@.exec'

files:
  - file: tryhex1.c
    commands: *c
    tests: $@.exec
  - file: tryhex2.c
    commands: *c
    tests: $@.exec
  - file: tryhex3.c
    commands: *c
    tests: $@.exec
  - file: prod_array.c
    commands: *c
    tests: $@.exec
  - file: prod_array2.c
    commands: *c
    tests: $@.exec
  - file: sum_array.c
    commands: *c
    tests: $@.exec
