---
assignment: lab5

compilers:
  - &c 'gcc $@ -o $@.exec'

files:
  - file: tryhex1.c
    commands: *c
    tests: $@.exec
  - file: tryhex2.c
    commands: *c
    tests: $@.exec
  - file: tryhex3.c
    commands: *c
    tests: $@.exec
  - file: strlenp.c
    commands: *c
    tests: $@.exec
