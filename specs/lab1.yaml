---
assignment: lab1

compilers:
  - &c 'gcc $@ -o $@.exec'

files:
  - file: hello.c
    commands: *c
    tests: $@.exec
  - file: hello.E
  - file: hello.o
    options:
      hide_contents: true
