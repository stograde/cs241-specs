---
assignment: lab9

compilers: 
  - &c-s 'gcc -c -S $@ -lpthread -lm'
  - &c-thread-math 'gcc $@ -o $@.exec -lpthread -lm'

files:
  - [ trap-omp.c, *c-thread-math ]
  - [ sum2.c, *c-s ]
  - [ sum2.s ]

tests:
  - [ trap-omp.c, $@.exec ]