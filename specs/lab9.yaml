---
assignment: lab9

compilers: 
  - &c-math 'gcc $@ -o $@.exec -lm'

files:
  - [ trybin.c, *c-math ]
  - [ getbit.c, *c-math ]

tests:
  - [ trybin.c, $@.exec ]
  - [ getbit.c, $@.exec < 37, $@.exec < F3 ]