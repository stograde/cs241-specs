---
assignment: lab9

compilers: 
  - &c-thread-math 'gcc $@ -o $@.exec -lpthread -lm'

files:
  - [ trap-omp.c, *c-thread-math ]
  - [ sum2.c ]
  - [ sum2.s ]

tests:
  - [ trap-omp.c, $@.exec ]
  